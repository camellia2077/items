# Docs Tools

This folder contains tooling that generates repository documentation artifacts.

Files:

- `generate_pickup_docs.py`: generates `docs/reference/pickups.md` from the grouped pickup catalog snapshot

Typical usage:

- `python .\tools\generate_pickup_docs.py`
